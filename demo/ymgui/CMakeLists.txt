# ymgui C++ demos — each program pushes ImGui frames as OSC to a running
# yetty instance. ymgui_frontend wraps Dear ImGui + ymgui_encode and writes
# to stdout (yetty's child PTY).
add_subdirectory(01_demo_window)
